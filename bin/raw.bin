#!/bin/bash

pkg install wget -y

#----------------------------------------
# Checks for shared commands, e.g. --help

check_common_commands()
{
    while [ $# -gt 0 ] ; do
        parm="$1"
        shift

        case "$parm" in
            --help)
            #usage
            echo -e "ayuda!! XD \n En proceso todavia"
            exit_success
            ;;

            --manual)
            exit_success
            ;;

            --version)
            echo "raw v1.0.0"
            exit_success
            ;;

            init)
            sudo apt install wget && curl proot tar 
            curl https://raw.githubusercontent.com/JIN26/install/main/package/install.pkg | bash
            exit_success
            ;;
        esac
    done
}